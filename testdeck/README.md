# testdeck

Drop a single `.pptx` file in this folder to test a one-deck review run from the **Path Input** in the app:

```
testdeck/
```

The app will pick up any `.pptx` file here. This folder is intentionally tracked in the repo so users have a known location to drop test decks.
